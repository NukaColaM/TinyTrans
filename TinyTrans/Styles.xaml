<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══ Colors ═══ -->
    <Color x:Key="WindowBackgroundColor">#F5F5F5</Color>
    <Color x:Key="OutputBackgroundColor">#EEEEEE</Color>
    <Color x:Key="WindowBorderColor">#CCCCCC</Color>
    <Color x:Key="ErrorColor">#C00000</Color>
    <Color x:Key="SubduedTextColor">#888888</Color>
    <Color x:Key="ButtonHoverColor">#E0E0E0</Color>
    <Color x:Key="ButtonPressedColor">#D0D0D0</Color>

    <!-- ═══ Brushes ═══ -->
    <SolidColorBrush x:Key="WindowBackground" Color="{StaticResource WindowBackgroundColor}" />
    <SolidColorBrush x:Key="OutputBackground" Color="{StaticResource OutputBackgroundColor}" />
    <SolidColorBrush x:Key="WindowBorder" Color="{StaticResource WindowBorderColor}" />
    <SolidColorBrush x:Key="ErrorForeground" Color="{StaticResource ErrorColor}" />
    <SolidColorBrush x:Key="SubduedForeground" Color="{StaticResource SubduedTextColor}" />
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="{StaticResource ButtonHoverColor}" />
    <SolidColorBrush x:Key="ButtonPressedBackground" Color="{StaticResource ButtonPressedColor}" />

    <!-- ═══ Icon Button Style ═══ -->
    <Style x:Key="IconButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="3"
                            BorderThickness="0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Content="{TemplateBinding Content}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonHoverBackground}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonPressedBackground}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.35" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
